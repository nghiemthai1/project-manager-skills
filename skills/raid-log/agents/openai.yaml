interface:
  display_name: "RAID Log"
  short_description: "Maintain distinct risks, assumptions, issues and dependencies wi"
  default_prompt: "Use $raid-log to maintain a dated RAID register whose classification, response, ownership and lifecycle follow the evidence rather than optimistic status labels."
