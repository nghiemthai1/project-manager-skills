interface:
  display_name: "Decision Log"
  short_description: "Record the exact choice, authority, rationale and conditions"
  default_prompt: "Use $decision-log to create durable decision records that distinguish proposals from authority and preserve bounded approvals, implementation state and later supersession."
