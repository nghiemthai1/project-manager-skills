interface:
  display_name: "Status Report"
  short_description: "Report project evidence, forecast variance and decisions needed."
  default_prompt: "Use $status-report to produce a dated audience-specific status report that preserves baseline, forecast, actual and acceptance states and makes the next intervention clear."
